# Don't push the Flutter SDK itself
flutter/

# Exclude unrelated project
painpoint/
untitled folder/

# macOS
.DS_Store

# Python backend
Backend-fastapi/.env
Backend-fastapi/**/__pycache__/
Backend-fastapi/**/*.pyc

# Flutter frontend (app folder is now named simtack)
simtack/.dart_tool/
simtack/build/
simtack/.flutter-plugins
simtack/.flutter-plugins-dependencies
simtack/android/.gradle
simtack/ios/Pods/
simtack/ios/.symlinks/

# Qoder / IDE tooling cache
.qoder/

# Local 3D model tooling/scratch (not part of the app)
model-source/
